---
import SectionContainer from '@/components/sectionContainer.astro';
import TextCite from '@/components/textCite.astro';

interface Props {
    h1_class?: string;
};

const { h1_class } = Astro.props;
---

<SectionContainer>
    <h1 class:list={["flex flex-col sm:gap-2 lg:gap-0 text-4xl text-center font-semibold 2xl:mt-14 text-yellow-200", h1_class]}>
        Encuentra
        <span class="text-slate-200 hover:text-slate-50 leading-[80px] lg:leading-tight not-italic text-[3.8rem] md:text-[5rem] my-4 md:my-0 xl:mb-1 font-extrabold transition duration-500">
            Toda la informacion
        </span>
        <cite class="text-purple-400">
            sobre Teclados Mecánicos
        </cite>
    </h1>
    <p class="text-2xl mt-10 2xl:mt-16 max-w-[1200px] text-pretty mx-auto text-center">
        Descubre <TextCite color="purple-300">por qué los teclados mecánicos son la elección preferida para gamers, profesionales y entusiastas.</TextCite> Desde interruptores únicos hasta diseños personalizables, <TextCite>aquí encontrarás todo lo que necesitas saber para elegir el teclado perfecto para ti.</TextCite> <TextCite color="purple-300">¡Mejora tu experiencia al escribir o jugar!</TextCite>
    </p>
</SectionContainer>