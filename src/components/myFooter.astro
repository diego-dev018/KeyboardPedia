---
import SectionContainer from "@/components/sectionContainer.astro";
import { me } from "@/data/me";
---

<SectionContainer
extraClass="flex !mt-0 !mb-0 bg-gray-800 bg-opacity-20 text-white justify-between items-center border-t-[1px] border-slate-800"
>
    <h3 class="text-2xl font-extrabold cursor-default">
        KeyBoardPedia
    </h3>
    <!-- <a
    class="text-2xl font-bold hover:bg-slate-800 hover:scale-105 px-4 py-2 rounded-full transition"
    href="https://www.diego18.pro/"
    target="_blank"
    >
        Mi Portfolio
    </a> -->
    <div>
        <h4 class="flex gap-2 text-2xl font-bold">
            Creado por 
            <a
            class="italic border-b-[1px] border-b-slate-700 hover:border-b-slate-400 hover:text-yellow-200 transition duration-500"
            href="https://www.diego18.pro"
            target="_blank"
            >Diego Sánchez</a>
        </h4>
        <div class="flex justify-between text-xl py-2 font-semibold">
            {
                me.socialNetworks.map((social) => (
                    <a
                    class="hover:text-blue-300 hover:scale-105 border-b-[1px] border-b-slate-800 hover:border-b-slate-400 transition"
                    href={social.url}
                    target="_blank"
                    >
                        {social.name}
                    </a>
                ))
            }
        </div>
    </div>
</SectionContainer>