---
interface Props {
    title?: string;
    description?: string;
    image_src: string;
}

const { title, description, image_src } = Astro.props;
---

<article
class="w-full aspect-square bg-[size:100%] hover:bg-[size:105%] bg-center rounded-xl transition-all"
style=`background-image: url('${image_src}');`
>
    <a
    href="/"
    class="w-full h-full flex flex-col items-end justify-end p-4"
    >
    <p
    class="text-white/60 text-lg font-semibold"
    >
        {title}
    </p>
    <h5
    class="text-white text-2xl font-bold"
    >
        {description}
    </h5>
    </a>
</article>